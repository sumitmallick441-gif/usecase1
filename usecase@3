public class HelloDefault {
    public static void main(String[] args) {
        // Set the default name
        String name = "World"; 
        
        // If an argument is provided, overwrite the default
        if (args.length > 0) {
            name = args[0]; 
        }
        
        System.out.println("Hello " + name);
    }
}
